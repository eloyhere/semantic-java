# semantic-java

Добро пожаловать в библиотеку `semantic-java`! 🚀 Эта библиотека предназначена для упрощения работы с коллекциями в Java и предоставляет ряд мощных инструментов для анализа данных, обработки потоков и других сценариев. Независимо от того, нужно ли вам проводить статистический анализ, работать с упорядоченными или неупорядоченными коллекциями, или определять пользовательские генераторы, `semantic-java` предоставит вам необходимые инструменты.

## Обзор

Библиотека `semantic-java` упрощает сложные операции с коллекциями, предоставляя высокоуровневые абстракции и практичные инструменты. Она сочетает в себе возможности функционального программирования, предлагая лаконичные и выразительные API.

## Основные возможности

- **Статистический анализ**: поддерживает расчет среднего значения, медианы, моды, дисперсии и других статистических показателей.
- **Упорядоченные и неупорядоченные коллекции**: обрабатывает коллекции с определенным порядком или без каких-либо ограничений на порядок.
- **Обработка потоков**: предоставляет API для обработки потоков, схожий с Java Streams, но с дополнительными функциями.
- **Пользовательские генераторы**: позволяет определять пользовательские генераторы для создания последовательностей элементов.

## Документация API

Ниже приведен подробный обзор основных API библиотеки `semantic-java` с примерами использования.

### 1. Collectable<E>

Класс `Collectable` является ядром библиотеки и предоставляет методы для сбора элементов на основе различных критериев.

#### Методы:

- `anyMatch(Predicate<E> predicate)`: Возвращает `true`, если хотя бы один элемент соответствует заданному условию.
- `allMatch(Predicate<E> predicate)`: Возвращает `true`, если все элементы соответствуют заданному условию.
- `noneMatch(Predicate<E> predicate)`: Возвращает `true`, если ни один элемент не соответствует заданному условию.
- `count()`: Возвращает количество элементов.
- `findFirst()`: Возвращает `Optional`, содержащий первый элемент.
- `findAny()`: Возвращает `Optional`, содержащий любой элемент.
- `forEach(BiConsumer<E, Long> consumer)`: Выполняет действие для каждого элемента.
- `groupBy(Function<E, K> keyExtractor)`: Группирует элементы по ключу.
- `join(String delimiter)`: Объединяет элементы в одну строку с использованием указанного разделителя.
- `print(OutputStream stream)`: Печатает элементы в указанный выходной поток.
- `reduce(BinaryOperator<E> operator)`: Уменьшает элементы с использованием заданного оператора.
- `toList()`: Преобразует коллекцию в `List`.
- `toMap(Function<E, K> keyExtractor, Function<E, V> valueExtractor)`: Преобразует коллекцию в `Map`.

#### Пример использования:

```java
Collectable<Integer> collectable = new Collectable<>(генератор);
boolean hasEven = collectable.anyMatch(n -> n % 2 == 0);
System.out.println("Существует четное число: " + hasEven);
```

### 2. Collector<A, E, R>

Класс `Collector` используется для накопления элементов в изменяемый результатный контейнер.

#### Методы:

- `collect(Generator<E> generator)`: Собирает элементы из генератора.
- `collect(Iterable<E> iterable)`: Собирает элементы из итерируемого объекта.
- `collect(E[] elements)`: Собирает элементы из массива.

#### Пример использования:

```java
Collector<Integer, Integer, Integer> collector = Collector.full(0, (acc, elem) -> acc + elem, Integer::sum);
int sum = collector.collect(generator);
System.out.println("Сумма: " + sum);
```

### 3. Statistics<E, D extends Number>

Класс `Statistics` предоставляет методы для вычисления статистических показателей.

#### Методы:

- `mean()`: Вычисляет среднее значение элементов.
- `median()`: Вычисляет медиану элементов.
- `mode()`: Вычисляет моду элементов.
- `variance()`: Вычисляет дисперсию элементов.
- `standardDeviation()`: Вычисляет стандартное отклонение элементов.

#### Пример использования:

```java
Statistics<Integer, Double> stats = new IntegerStatistics<>(генератор);
double average = stats.mean();
System.out.println("Среднее значение: " + average);
```

### 4. OrderedCollectable<E>

Класс `OrderedCollectable` расширяет `Collectable` и предоставляет методы для работы с упорядоченными коллекциями.

#### Методы:

- `sorted()`: Сортирует элементы.
- `sorted(Comparator<E> comparator)`: Сортирует элементы с использованием заданного компаратора.

#### Пример использования:

```java
OrderedCollectable<Integer> ordered = new OrderedCollectable<>(генератор);
List<Integer> sortedList = ordered.sorted().toList();
System.out.println("Отсортированный список: " + sortedList);
```

### 5. UnorderedCollectable<E>

Класс `UnorderedCollectable` расширяет `Collectable` и предоставляет методы для работы с неупорядоченными коллекциями.

#### Методы:

- `toUnordered()`: Преобразует коллекцию в неупорядоченное представление.

#### Пример использования:

```java
UnorderedCollectable<Integer> unordered = new UnorderedCollectable<>(генератор);
unordered.toUnordered();
```

### 6. Window<E>

Класс `Window` предоставляет методы для создания скользящих или перекатывающихся окон над коллекцией.

#### Методы:

- `slide(long size, long step)`: Создает скользящее окно заданного размера и шага.
- `tumble(long size)`: Создает перекатывающее окно заданного размера.

#### Пример использования:

```java
Window<Integer> window = new Window<>(генератор);
Semantic<Semantic<Integer>> slidingWindow = window.slide(3, 1);
```

## Сравнение с Java Streams

| Функциональность               | semantic-java                           | Java Streams                           |
|--------------------------------|-----------------------------------------|----------------------------------------|
| Статистический анализ          | Поддерживается                          | Ограничено (требуется внешняя библиотека)|
| Работа с упорядоченными и неупорядоченными коллекциями | Поддерживается                          | Ограничено (Streams по умолчанию упорядочены)|
| Пользовательские генераторы    | Поддерживается                          | Не поддерживается                       |
| Операции с окнами              | Поддерживается (скользящие и перекатывающиеся окна) | Не поддерживается                       |
| Оптимизация производительности | Оптимизировано для конкретных сценариев  | Общего назначения, может быть не оптимизировано для конкретных задач |

## Особые указания

- **toUnordered**: Этот метод не учитывает порядок элементов и оптимизирован для производительности. Подходит для сценариев, когда порядок элементов не важен.
- **toXXX (например, toList, toMap)**: Эти методы учитывают порядок элементов и подходят для сценариев, когда последовательность важна.
- **Операции с индексами**: Если после вызова `redirect`, `reverse` или `translate` вызвать `sorted`, операции с индексами могут стать недействительными, так как сортировка перезаписывает эти операции.

## Заключение

`semantic-java` - это мощная библиотека для обработки коллекций в Java, сочетающая в себе функциональное программирование и оптимизацию производительности. Независимо от того, нужно ли вам проводить сложный статистический анализ, эффективно обрабатывать большие наборы данных или требуется больше контроля над коллекциями, `semantic-java` предоставляет необходимые инструменты.

Мы надеемся, что эта библиотека будет полезной для вас, и ждем вашего отзыва! 🌟

---

*Это руководство по использованию является отправной точкой, и по мере развития библиотеки мы будем предоставлять более подробную документацию и примеры.*
